Phase 0E-R3 static/source audit
Date: 2026-08-11
Result: PASS

1. Legacy user metadata key
Command: rg -n "__phase0e_r1_group_positions" crates shared web
Result: two negative assertions only:
- crates/document/tests/phase0e_group.rs
- crates/serialization/src/lib.rs
No production write or read of the legacy key exists.

2. Untracked Rust bulk constructor
Command: rg -n "OrderSequence::from_ids\(" crates
Result: zero matches.

3. Tracked Rust bulk constructor
Command: rg -n "OrderSequence::from_ids_tracked" crates
Result: Document, Scene, restoration, and tests use the tracked constructor.

4. UI constructor boundary
Command: rg -n "RankedSequence\.from|new RankedSequence" web/editor/src web/editor/tests
Result: bulk creation uses RankedSequence.from(values, work); empty construction uses new RankedSequence() and allocates no nodes.

5. Complete counters
Result: Rust SequenceWork, WASM RequestMetrics, TypeScript SequenceWork, UiCounters, matrix reports, and browser proof include examined/copied/moved, comparisons, nodes, bytes, rotations/rebalances, maximum depth, fallback/rebuild, full scans/copies, and dense rewrites.

6. Structural fallback and full-work boundaries
Result: all five 70-case layers report full scans=0, full copies=0, dense rewrites=0, fallback/full rebuilds=0. Actual Chrome reports maximum fallback 0.

7. Evidence boundary
Result: direct WASM evidence records WebAssembly.Module=true, WebAssembly.Instance=true, glue instance=true, native_binary_used=false, artifact hash, artifact bytes, protocol version, render schema, Node and V8 versions.

8. Preservation
Result: prior documents/evidence changed=0; authoritative files 19/19 byte-identical; authoritative SHA256SUMS 18/18.
